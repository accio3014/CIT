#include <stdio.h>

int main() {
    // 반복문
    // C언어에서 반복문은 for, while 2가지가 있음
    // 둘다 동일한 반복을 할 수 있고 약간의 차이가 있어, 편한 것을 사용하면 됨

    // for
    // for(초기화; 조건; 증감/감소) {
    //      반복할 코드
    // }

    // for(int i=0; i<5; i++) {    // 5번 반복
    //     printf("%d \n", i);
    // }

    return 0;
}